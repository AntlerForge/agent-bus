# W3.2 isolated Borg restore drill manifest

Declared at: 2026-07-19T20:14:30Z

Archive selected before restore: `antler-a6-2026-07-19T200436Z` from
`/mnt/backup/borg/a6-primary`. Selection rule: newest completed archive existing before
this declaration. The archive was created with a Borg warning because a transient Agent Bus
atomic-write temporary file disappeared during traversal; the archive itself completed and is
addressable. This is recorded rather than hidden.

Restore root: a new `mktemp -d` child under
`/srv/projects/Personal/agent-bus/runtime/restore-drills/`. No production destination is an
allowed extraction target.

Predeclared assets:

1. `/srv/kv/vault/dashboard/doctor.json` — small KV read model; must parse as JSON.
2. `/srv/projects/Personal/agent-bus/app/scripts/decision-queue.mjs` — executable component;
   must pass `node --check` from the restored path.
3. `/srv/projects/Personal/agent-bus/app/config/decision-queue-sla.v1.yaml` — service policy
   configuration; must parse as YAML.
4. `/etc/samba/smb.conf` — service configuration and explicit non-`/srv` Borg source.
5. `/share/Knowledge-Vault/tasks/day-board.md` — explicit `/share` coverage proof.

Verification declared before restore:

- Record SHA-256 for each production asset immediately before extraction.
- Extract only the five paths above with Borg into the isolated root.
- Match restored hashes to the recorded production hashes.
- Prove production hashes are unchanged after the drill.
- Functionally parse JSON and YAML, syntax-check the restored executable, and serve the
  restored `doctor.json` through a loopback-only ephemeral HTTP server before fetching and
  parsing it as a client.
- Record archive time, drill start/end, elapsed RTO, and observed RPO in a JSON result.
