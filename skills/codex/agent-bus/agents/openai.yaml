interface:
  display_name: "Agent Bus"
  short_description: "Coordinate governed work across agent runtimes"
  default_prompt: "Use $agent-bus to coordinate this work through the A6 control plane and report the next approval or bridge action."
