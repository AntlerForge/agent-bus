interface:
  display_name: "Agent Bus"
  short_description: "Use local Agent Bus mailbox for Claude-Codex task handoff."
  default_prompt: "Use Agent Bus to check your inbox, answer actionable messages, and send replies through the mailbox."
